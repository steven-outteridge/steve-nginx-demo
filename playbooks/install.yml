- name: Install 
  hosts: testserver
  sudo: True
  tasks:
    - name: Install Docker
      yum:
        name: docker
        state: latest

    - name: start docker
      service: name=docker state=started

